#include < iostream >
using namespace std ;
main ()
{
int a , b ;
char c ; 
cout << " enter two numbers and opera tar " : ; 
cin >> a >> b >> c ;
switch ( c)
{
case ' + ' : cout << " sum = " << a + b << endl ; break ;
case ' - ' : cout << " minus = " << a - b << endl ; break ;
case ' * ' : ciut << " star = " << a * b << endl ; break;
case ' / ' : cout << " divid = " << a / b << endl ; break ; 
case ' % ' : cout << " mod = " << endl ; break ;
}
}
